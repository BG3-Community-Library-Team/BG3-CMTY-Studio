name: 'Setup Rust'
description: 'Install Rust toolchain via rust-toolchain.toml and apply cargo cache'
runs:
  using: composite
  steps:
    - uses: dtolnay/rust-toolchain@stable
    - uses: Swatinem/rust-cache@v2
      with:
        workspaces: src-tauri
